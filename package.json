{
  "name": "nodews",
  "version": "1.0.0",
  "description": "Nodejs-server",
  "main": "index.js",
  "private": false,
  "scripts": {
    "start": "node index.js",
    "build": "npx -y esbuild index.js --bundle --minify --sourcemap --outfile=dist/bundle.js --platform=node"
  },
  "dependencies": {
    "ws": "^8.14.2",
    "axios": "^1.12.2"
  },
  "engines": {
    "node": ">=14"
  }
}
